import Orientation from '../../components/Orientation/Orientation';
import { ORIENTATION } from '../namespaces';

export default {
    path: ORIENTATION,
    exact: true,
    name: 'Orientation',
    component: Orientation,
};
